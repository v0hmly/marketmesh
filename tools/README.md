# Инструменты репозитория

Каталог содержит локальную и CI-автоматизацию. Скрипт `go-workspace.sh` запускает одинаковые Go-проверки во всех модулях и проверяет разрешённый граф зависимостей. Taskfile и Makefile являются двумя интерфейсами к этому скрипту и не содержат собственных списков модулей.

// Package tunnel implements the DMZ endpoint of the MarketMesh reverse tunnel.
// It accepts authenticated gateway-out streams and exposes only statically
// allowlisted logical routes to other gateway-in adapters.
package tunnel

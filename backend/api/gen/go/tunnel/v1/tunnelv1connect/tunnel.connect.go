// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tunnel/v1/tunnel.proto

package tunnelv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/v0hmly/marketmesh/api/gen/go/tunnel/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TunnelServiceName is the fully-qualified name of the TunnelService service.
	TunnelServiceName = "tunnel.v1.TunnelService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TunnelServiceConnectProcedure is the fully-qualified name of the TunnelService's Connect RPC.
	TunnelServiceConnectProcedure = "/tunnel.v1.TunnelService/Connect"
)

// TunnelServiceClient is a client for the tunnel.v1.TunnelService service.
type TunnelServiceClient interface {
	// Connect multiplexes bounded logical requests over one bidirectional gRPC stream.
	Connect(context.Context) *connect.BidiStreamForClient[v1.ConnectRequest, v1.ConnectResponse]
}

// NewTunnelServiceClient constructs a client for the tunnel.v1.TunnelService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTunnelServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TunnelServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	tunnelServiceMethods := v1.File_tunnel_v1_tunnel_proto.Services().ByName("TunnelService").Methods()
	return &tunnelServiceClient{
		connect: connect.NewClient[v1.ConnectRequest, v1.ConnectResponse](
			httpClient,
			baseURL+TunnelServiceConnectProcedure,
			connect.WithSchema(tunnelServiceMethods.ByName("Connect")),
			connect.WithClientOptions(opts...),
		),
	}
}

// tunnelServiceClient implements TunnelServiceClient.
type tunnelServiceClient struct {
	connect *connect.Client[v1.ConnectRequest, v1.ConnectResponse]
}

// Connect calls tunnel.v1.TunnelService.Connect.
func (c *tunnelServiceClient) Connect(ctx context.Context) *connect.BidiStreamForClient[v1.ConnectRequest, v1.ConnectResponse] {
	return c.connect.CallBidiStream(ctx)
}

// TunnelServiceHandler is an implementation of the tunnel.v1.TunnelService service.
type TunnelServiceHandler interface {
	// Connect multiplexes bounded logical requests over one bidirectional gRPC stream.
	Connect(context.Context, *connect.BidiStream[v1.ConnectRequest, v1.ConnectResponse]) error
}

// NewTunnelServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTunnelServiceHandler(svc TunnelServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tunnelServiceMethods := v1.File_tunnel_v1_tunnel_proto.Services().ByName("TunnelService").Methods()
	tunnelServiceConnectHandler := connect.NewBidiStreamHandler(
		TunnelServiceConnectProcedure,
		svc.Connect,
		connect.WithSchema(tunnelServiceMethods.ByName("Connect")),
		connect.WithHandlerOptions(opts...),
	)
	return "/tunnel.v1.TunnelService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TunnelServiceConnectProcedure:
			tunnelServiceConnectHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTunnelServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTunnelServiceHandler struct{}

func (UnimplementedTunnelServiceHandler) Connect(context.Context, *connect.BidiStream[v1.ConnectRequest, v1.ConnectResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("tunnel.v1.TunnelService.Connect is not implemented"))
}

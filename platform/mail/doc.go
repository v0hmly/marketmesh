// Package mail рендерит транзакционные письма MarketMesh из встроенных шаблонов.
//
// Шаблоны повторяют прототипы docs/design/prototypes/Email*.dc.html: табличная
// вёрстка с инлайн-стилями и захардкоженными hex (CSS-переменные почтовые
// клиенты не поддерживают), Georgia в заголовках, тёмная тема через
// color-scheme и отдельный медиазапрос. Каждое письмо имеет HTML и plaintext
// части; плейсхолдеры — поля типизированных data-структур ({{ .Field }}),
// экранирование выполняет html/template.
//
// Пакет только рендерит: отправка (SMTP, Mailpit в локальном контуре) — за
// пределами этого пакета.
package mail

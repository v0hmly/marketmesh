One row, three slots: the wordmark text in `text-primary`, the tagline "Сделано людьми. Для людей." (two short parallel sentences, matching the hero's "Изделия мастеров. Личные истории."), and a right-aligned `.footer-meta` naming the current area. A single `border-subtle` rule separates it from the page.

`.footer-meta` is hidden below 720px, so treat it as a bonus rather than information — the same rule that applies to the page heading's section number. The row wraps rather than scrolls when the three slots don't fit.
